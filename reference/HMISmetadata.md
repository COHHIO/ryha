# HMISmetadata

Lists HMIS .csv files and whether or not they are required to develop
the app

## Usage

``` r
HMISmetadata
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 25
rows and 2 columns.
