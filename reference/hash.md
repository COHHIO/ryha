# Hash a value with a key

Hash a value with a key

## Usage

``` r
hash(x, key)
```

## Arguments

- x:

  A character string

- key:

  A hashing dictionary key

## Value

An hashed string

## Examples

``` r
if (FALSE) { # \dontrun{

hash("blah", key = my_key)
} # }
```
