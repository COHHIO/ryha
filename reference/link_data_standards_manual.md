# Create a Link to the HMIS Data Standards Manual

`link_data_standards_manual()` generates an HTML anchor (`<a>`) tag that
links to the HMIS Data Standards Manual PDF.

## Usage

``` r
link_data_standards_manual()
```

## Value

An HTML anchor (`<a>`) tag as a `shiny.tag` object.
